# Runtime dependencies for the dashboard app only (dashboard/*.py).
# Does NOT cover generating data/processed/claims_{mode}.parquet — that's a
# one-time step using the main repo's own dependencies (pandas, pyarrow,
# requests). See dashboard/README.md.
dash>=4.4
dash-bootstrap-components>=2.0
gunicorn>=26.0
numpy>=2.4
plotly>=6.9
polars>=1.42
